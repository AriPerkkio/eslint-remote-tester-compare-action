name: "test"

on:
  issue_comment:
    types: [created, edited]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - uses: ./
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
